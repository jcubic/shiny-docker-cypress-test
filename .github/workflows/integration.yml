name: Integration-Tests
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: build docker
        run: make build
      - name: Test
        run: make test
